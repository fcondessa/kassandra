from distutils.core import setup
setup(
  name = 'kassandra',
  packages = ['kassandra'], # this must be the same as the name above
  version = '0.1',
  description = 'A library that implements a tensorial convolutional predictor',
  author = 'Filipe Condessa',
  author_email = 'fcondessa@gmail.com',
  url = 'https://github.com/fcondessa/kassandra', # use the URL to the github repo
  download_url = 'https://github.com/fcondessa/kassandra/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['machine learning', 'forecasting', 'prediction'], # arbitrary keywords
  classifiers = [],
)